<% if(type === '30min') { %>
    Your meeting with <%= name %> is starting in 30 minutes
<% } else if(type === '24hour') { %>
    Your meeting with <%= name %> is starting in 24 hours
<% } else { %>
    Reminder: <%= name %> scheduled for <%= current_job_doc.job_date_time %>
<% } %>