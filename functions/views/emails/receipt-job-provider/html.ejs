<%- include('../html-header.ejs') %>

                      <div class="f-fallback">
                        <h1>Hi, <%= current_job_doc.customer_name %>!</h1>
                        <p>You received a payment of $<%= (receipt.provider_received /100).toFixed(2) %> from <%= current_job_doc.customer_name %>. 
                          Funds are usually available for transfer to your bank account in 1-2 business days.</p>
                        <table class="purchase" width="100%" cellpadding="0" cellspacing="0" role="presentation">
                          <tr>
                            <td>
                              <h3>Invoice# <%= receipt_id %></h3></td>
                            <td>
                              <h3 class="align-right"><%= receipt_date %></h3></td>
                          </tr>
                          <tr>
                            <td colspan="2">
                              <table class="purchase_content" width="100%" cellpadding="0" cellspacing="0">
                                <tr>
                                  <th class="purchase_heading" align="left">
                                    <p class="f-fallback">Description</p>
                                  </th>
                                  <th class="purchase_heading" align="right">
                                    <p class="f-fallback">Amount</p>
                                  </th>
                                </tr>
                                <tr>
                                  <td width="80%" class="purchase_item">
                                    <span class="f-fallback">
                                    <%= current_job_doc.topic  %>: <%= current_job_doc.job_date_time %> for 
                                    <%= (receipt.lengthInSeconds/60).toFixed(2)  %> minutes @ <%= current_job_doc.rate_name %> per hour
                                  </span>
                                </td>
                                  <td class="align-right" width="20%" class="purchase_item"><span class="f-fallback">$<%= (receipt.total_paid /100).toFixed(2)  %></span></td>
                                </tr>
                                <tr>
                                  <td width="80%" class="purchase_item">
                                    <span class="f-fallback">
                                    Service fee
                                  </span>
                                </td>
                                  <td class="align-right" width="20%" class="purchase_item"><span class="f-fallback">
                                    ($<%= ((receipt.total_paid - receipt.provider_received) /100).toFixed(2)  %>)</span></td>
                                </tr>

                                <tr>
                                  <td width="80%" class="purchase_footer" valign="middle">
                                    <p class="f-fallback purchase_total purchase_total--label">Total</p>
                                  </td>
                                  <td width="20%" class="purchase_footer" valign="middle">
                                    <p class="f-fallback purchase_total">$<%= (receipt.provider_received /100).toFixed(2)  %></p>
                                  </td>
                                </tr>
                              </table>
                            </td>
                          </tr>
                        </table>

                        <p>If you have any questions, please <a href="mailto:<%= support_email %>">email our customer success team</a>.</p>
                        <p>Thanks,
                          <br>The <%= product_name %> Team</p>
                        
                      </div>


<%- include('../html-footer.ejs') %>